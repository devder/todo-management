import type { NextPage } from "next";
import { Layout } from "components/layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <div className="container">Test</div>
      <h1>Testing </h1>
    </Layout>
  );
};

export default Home;
